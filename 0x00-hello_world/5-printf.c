#include <stdio.h>
/**
 *main - A function that prints a string with printf funtion.
 *Return: Always 0 (success)
 */
int main(void)
{
	puts("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
