#include "main.h"
/**
 *mul -a function that prints
 *the product of two numbers
 *@a: A number
 *@b: another number
 *Return: 0 (a * b)
 */

int mul(int a, int b)
{
 int result = (a * b);

	return (result);
}
